HOW TO USE

-Make sure AWS config data is populated correctly before using i.e. make sure the bucket name matches your created bucket
-Make sure the S3 Bucket actually exists
-The file to be read/downloaded/uploaded is hard-coded to "TestFile.txt"
-Make sure the file you want to upload is in the Resources folder
-After downloading a file, right click in Editor and press "Refresh" to see it